[project]
name = "dailyvocchatbot"
version = "0.1.0"
description = "An AI-powered chatbot that sends 5 new vocabulary words daily to users via Email or Telegram."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
